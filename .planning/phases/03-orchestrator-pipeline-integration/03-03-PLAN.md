---
phase: 03-orchestrator-pipeline-integration
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - api-connector-app/src/test/java/com/suntek/apiconnector/app/LegacyCompatIntegrationTest.java
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "A legacy thirdpart URL and the unified API hit the SAME orchestrator pipeline for an equivalent endpoint (D-06)"
    - "Legacy and unified produce the same CORE result: success, vendorCode, mapped data, body — outer envelope may differ (D-08)"
    - "LegacySpecialHandler/IdpsLegacySpecialHandler act as pre/post adapters only and never replace mapping (D-07)"
    - "Business error JSON shape is owned by mapping.error once; LegacyCompatResponseFormatter handles transport envelope only — no double error mapping (D-09)"
  artifacts:
    - path: "api-connector-app/src/test/java/com/suntek/apiconnector/app/LegacyCompatIntegrationTest.java"
      provides: "Parity test (legacy vs unified core result) + envelope-only error guard"
      contains: "legacyAndUnifiedCoreResultParity"
  key_links:
    - from: "ThirdpartLegacyDispatcher.forward"
      to: "IntegrationInvokeService.invoke(code3rd, request, InvokeContext.LEGACY)"
      via: "shared service entry — same orchestrator.invoke(), no parallel pipeline"
      pattern: "InvokeContext\\.LEGACY"
    - from: "LegacyCompatIntegrationTest.legacyAndUnifiedCoreResultParity"
      to: "unified + legacy invoke of the same endpoint"
      via: "assert equal success/vendorCode/data, not byte-identity of envelope"
      pattern: "assert"
---

<objective>
Prove PIPE-02: legacy thirdpart URL routes go through the exact same orchestration pipeline as the unified API, producing an identical core result. Per D-06 this requires NO new pipeline code — `ThirdpartLegacyDispatcher` already delegates to `IntegrationInvokeService.invoke(..., InvokeContext.LEGACY)`, so wiring the orchestrator once in 03-01 gives legacy mapping/transform/auth for free. This slice is the guard against divergence: a parity integration test (D-08) and an envelope-only error guard (D-09).

Purpose: PIPE-02's risk is someone reimplementing mapping inside the legacy adapter (Pitfall 5) or double-mapping errors. The test is the contract that keeps legacy a thin adapter over the shared pipeline.
Output: A green `legacyAndUnifiedCoreResultParity` test plus an error envelope-only guard in `LegacyCompatIntegrationTest`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-orchestrator-pipeline-integration/03-CONTEXT.md
@.planning/phases/03-orchestrator-pipeline-integration/03-RESEARCH.md
@.planning/phases/03-orchestrator-pipeline-integration/03-PATTERNS.md
@.planning/phases/03-orchestrator-pipeline-integration/03-VALIDATION.md
@docs/THIRDPART-MIGRATION.md
@api-connector-api/src/main/java/com/suntek/apiconnector/api/legacy/ThirdpartLegacyDispatcher.java
@api-connector-api/src/main/java/com/suntek/apiconnector/api/legacy/LegacyCompatResponseFormatter.java
@api-connector-app/src/test/java/com/suntek/apiconnector/app/LegacyCompatIntegrationTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Legacy vs unified core-result parity test (D-06/D-08)</name>
  <files>api-connector-app/src/test/java/com/suntek/apiconnector/app/LegacyCompatIntegrationTest.java</files>
  <action>Add `legacyAndUnifiedCoreResultParity` to `LegacyCompatIntegrationTest`. Analog: the existing `legacyIdpsGetProxiesVendorJson` (l.74-85) for the legacy URL call and `rebindIdpsToWireMock` (l.52-72) to point a catalog connector at WireMock, with `integration.legacy.enabled=true` already set in `@DynamicPropertySource` (l.49). Register ONE endpoint that has a request+response mapping configured, then invoke it twice against the same WireMock stub: (a) via the unified API `POST /api/v1/integrations/{code3rd}/endpoints/{id}/invoke`, and (b) via its legacy thirdpart URL. Assert the two responses share the same CORE result — `success`, `vendorCode`, parsed/ mapped `data`, and mapped `body` — proving both ran mapping/transform/auth through the same `orchestrator.invoke()` (D-06). Do NOT assert byte-identity of the outer envelope: the `LegacySuntekResult` shape via `LegacyCompatResponseFormatter` is allowed to differ (D-08). This test depends on 03-01's wiring; it goes green once the orchestrator applies mapping.</action>
  <verify>
    <automated>.\mvnw-jdk21.ps1 -pl api-connector-app -am test -Dtest=LegacyCompatIntegrationTest#legacyAndUnifiedCoreResultParity</automated>
  </verify>
  <done>The legacy URL and unified API return equal `success`, `vendorCode`, and mapped `data`/`body` for the same endpoint; the test passes against the 03-01 pipeline; envelope differences are explicitly NOT asserted.</done>
</task>

<task type="auto">
  <name>Task 2: No-double-mapping error envelope guard (D-07/D-09)</name>
  <files>api-connector-app/src/test/java/com/suntek/apiconnector/app/LegacyCompatIntegrationTest.java</files>
  <action>Add `legacyErrorMappedOnceEnvelopeOnly` to `LegacyCompatIntegrationTest`. Configure the connector with a `mapping.error` rule and a WireMock stub that returns a vendor business error (non-2xx or `success=false` body). Invoke via the legacy URL and assert: (1) the business error JSON shape matches the `mapping.error` output exactly once — i.e. the error fields are the mapped legacy shape, NOT re-transformed a second time by the formatter; (2) `LegacyCompatResponseFormatter` only contributed the transport-layer envelope (HTTP status / Content-Type / `LegacySuntekResult` wrapper), not a second business-error transform (D-09). While writing the test, READ `ThirdpartLegacyDispatcher` and `IdpsLegacySpecialHandler`/`LegacySpecialHandler` to confirm they run purely as pre/post adapters and do NOT contain mapping/transform calls (D-07, Pitfall 5 — mapping logic must NOT appear under `api/legacy/*`). If a divergence is found (an adapter replacing mapping, or the formatter double-mapping the business error), FIX it in the named legacy file, add that file to the summary's modified list, and note it; otherwise the test stands as the regression guard with no production change.</action>
  <verify>
    <automated>.\mvnw-jdk21.ps1 -pl api-connector-app -am test -Dtest=LegacyCompatIntegrationTest</automated>
  </verify>
  <done>The legacy error response carries the `mapping.error` business shape exactly once; the formatter is confirmed envelope-only; no mapping/transform calls exist in `api/legacy/*` adapters. Full `LegacyCompatIntegrationTest` is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy thirdpart URL -> ThirdpartLegacyDispatcher | Legacy callers enter via raw URLs; the dispatcher must funnel them through the same access posture as the unified API. |
| shared orchestrator pipeline -> legacy response formatter | The mapped result crosses into the legacy envelope formatter, which must not re-transform business data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | legacy URL bypassing validation/strict-endpoints the unified path enforces | mitigate | D-06/D-07: legacy routes through the SAME `IntegrationInvokeService.invoke(..., InvokeContext.LEGACY)`; adapters do not skip auth/mapping/strict-endpoints. `legacyAndUnifiedCoreResultParity` guards against divergence into a parallel pipeline. |
| T-03-06 | Information Disclosure | double-mapped / re-transformed legacy error response | mitigate | D-09: `mapping.error` owns the business error JSON once; `LegacyCompatResponseFormatter` is envelope-only. `legacyErrorMappedOnceEnvelopeOnly` asserts a single mapping; adapters confirmed to contain no mapping calls. |
| T-03-02 | Information Disclosure | legacy invoke logs | accept | Legacy path reuses the same audit/log code (03-02); no body/credential logging is introduced here. No new log surface in this test-only plan. |
</threat_model>

<verification>
- Parity: `.\mvnw-jdk21.ps1 -pl api-connector-app -am test -Dtest=LegacyCompatIntegrationTest#legacyAndUnifiedCoreResultParity`
- Full legacy compat: `.\mvnw-jdk21.ps1 -pl api-connector-app -am test -Dtest=LegacyCompatIntegrationTest`
- Confirm by reading `api/legacy/*` that no mapping/transform call exists in the adapters (D-07).
</verification>

<success_criteria>
- ROADMAP SC#2: the legacy compat filter URL hits the same pipeline with an identical core outcome as the unified API for an equivalent endpoint.
- Legacy and unified core results (success, vendorCode, mapped data/body) are equal; envelope difference allowed (D-08).
- No mapping/transform logic exists under `api/legacy/*`; error JSON shape mapped exactly once (D-07/D-09).
</success_criteria>

<output>
Create `.planning/phases/03-orchestrator-pipeline-integration/03-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

NEW symbols/changes introduced by this plan:
- New test methods in `LegacyCompatIntegrationTest`: `legacyAndUnifiedCoreResultParity` (D-06/D-08), `legacyErrorMappedOnceEnvelopeOnly` (D-09).
- No new production class expected (D-06 — legacy already delegates to the shared service). Any divergence fix in `api/legacy/*` is contingency-only and recorded in the SUMMARY if it occurs.
