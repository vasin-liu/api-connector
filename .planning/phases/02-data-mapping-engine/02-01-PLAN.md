---
phase: 02-data-mapping-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - api-connector-dependencies/pom.xml
  - api-connector-mapping/pom.xml
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/MappingSpec.java
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/DirectionMappingSpec.java
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/MappingRule.java
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/ConnectorSpec.java
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/EndpointSpec.java
  - api-connector-spec/src/main/java/com/suntek/apiconnector/spec/ConnectorSpecParser.java
  - api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidator.java
  - api-connector-spec/src/test/java/com/suntek/apiconnector/spec/ConnectorSpecParserMappingTest.java
  - api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidatorTest.java
  - api-connector-mapping/src/test/resources/mapping/demo-rename.yaml
autonomous: true
requirements:
  - MAP-01
  - MAP-05
must_haves:
  truths:
    - "ConnectorSpec parses mapping.request, mapping.response, mapping.error blocks from YAML/JSON"
    - "Endpoint mappingOverride wins per direction over connector mapping (D-01)"
    - "Parser rejects direction block containing both rules and script (D-10)"
    - "MappingSpecValidator rejects invalid JSONPath on publish/load (D-04, D-30)"
    - "toConnectorMap and endpointToMap round-trip mapping fields without data loss (D-27, Pitfall 8)"
  artifacts:
    - path: api-connector-mapping/pom.xml
      provides: New mapping module depending on domain, spec, scripting, json-path
      min_lines: 25
    - path: api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/MappingSpec.java
      provides: Connector-level mapping model with request/response/error directions
      contains: "request"
    - path: api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidator.java
      provides: Publish-time JSONPath compile and known-op validation
      contains: "validate"
    - path: api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidatorTest.java
      provides: Invalid JSONPath rejection test (ROADMAP SC#6)
      min_lines: 30
  key_links:
    - from: api-connector-spec/src/main/java/com/suntek/apiconnector/spec/ConnectorSpecParser.java
      to: MappingSpec
      via: parse mapping and mappingOverride from connector/endpoint maps
      pattern: "mapping"
    - from: api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidator.java
      to: com.jayway.jsonpath.JsonPath
      via: JsonPath.compile on each source/target path
      pattern: "JsonPath\\.compile"
    - from: api-connector-spec/src/test/java/com/suntek/apiconnector/spec/ConnectorSpecParserMappingTest.java
      to: ConnectorSpecParser.toConnectorMap
      via: parse → serialize → parse equality on mapping block
      pattern: "toConnectorMap"
---

## Phase Goal

**As an** integration operator, **I want** mapping rules defined in connector spec YAML with publish-time validation, **so that** invalid paths and transforms are rejected before runtime (MAP-01 schema, MAP-05 JDBC round-trip, D-01..D-04, D-10, D-27, D-30).

<objective>
Scaffold `api-connector-mapping` module and extend spec models/parser with `mapping` / `mappingOverride` blocks plus `MappingSpecValidator`.

Purpose: Walking-skeleton foundation for declarative DSL; symmetric JDBC blob persistence without new tables (D-27).
Output: Module compiles; parser round-trip tests green; validator rejects bad JSONPath.
</objective>

<execution_context>
@C:/Users/vasin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/vasin/.claude/gsd-core/templates/summary.md
@.planning/phases/02-data-mapping-engine/02-SKELETON.md
</execution_context>

<context>
@.planning/phases/02-data-mapping-engine/02-CONTEXT.md
@.planning/phases/02-data-mapping-engine/02-RESEARCH.md
@.planning/phases/02-data-mapping-engine/02-PATTERNS.md
@docs/adr/002-auth-and-transform.md
@api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/ConnectorSpec.java
@api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/EndpointSpec.java
@api-connector-spec/src/main/java/com/suntek/apiconnector/spec/ConnectorSpecParser.java
@api-connector-engine/src/main/java/com/suntek/apiconnector/engine/ResponseEvaluator.java
@pom.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold api-connector-mapping module and spec models</name>
  <files>pom.xml, api-connector-dependencies/pom.xml, api-connector-mapping/pom.xml, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/MappingSpec.java, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/DirectionMappingSpec.java, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/MappingRule.java, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/ConnectorSpec.java, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/EndpointSpec.java</files>
  <read_first>pom.xml, api-connector-auth/pom.xml, api-connector-spec/src/main/java/com/suntek/apiconnector/spec/model/ConnectorSpec.java, .planning/phases/02-data-mapping-engine/02-PATTERNS.md</read_first>
  <action>
    Add `api-connector-mapping` to root pom.xml module list (after `api-connector-auth`).
    Create `api-connector-mapping/pom.xml` with parent `api-connector-parent`, artifactId `api-connector-mapping`.
    Dependencies: `api-connector-domain`, `api-connector-spec`, `api-connector-scripting`, `com.jayway.jsonpath:json-path`, JUnit 5 for tests. No dependency on engine or Spring Web (hexagonal rule).
    Add BOM entry in `api-connector-dependencies/pom.xml` for `api-connector-mapping`.
    Create `MappingRule` record: fields `op`, `source`, `target`, `type` (for coerce), `value` (for set), `rules` (nested list for array_map).
    Create `DirectionMappingSpec` record: optional `List&lt;MappingRule&gt; rules`, optional `String script` (per-direction Groovy source).
    Create `MappingSpec` record: optional `DirectionMappingSpec request`, `response`, `error` (D-03).
    Extend `ConnectorSpec` with `MappingSpec mapping` field and accessor; extend constructor.
    Extend `EndpointSpec` with `MappingSpec mappingOverride` field (full per-direction block override, mirror auth override pattern but full block per D-01 research recommendation).
    Known ops enum constant set: `rename`, `coerce`, `nest`, `array_map`, `set` (D-02).
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-mapping,api-connector-spec -am compile</verify>
  <acceptance_criteria>
    - Root pom.xml lists module `api-connector-mapping`
    - MappingSpec.java defines request/response/error direction fields
    - ConnectorSpec.java exposes mapping() accessor
    - EndpointSpec.java exposes mappingOverride() accessor
    - api-connector-mapping/pom.xml has no dependency on api-connector-engine
  </acceptance_criteria>
  <done>Module and spec models compile</done>
</task>

<task type="auto">
  <name>Task 2: Extend ConnectorSpecParser parse/serialize and mutual exclusion</name>
  <files>api-connector-spec/src/main/java/com/suntek/apiconnector/spec/ConnectorSpecParser.java, api-connector-spec/src/test/java/com/suntek/apiconnector/spec/ConnectorSpecParserMappingTest.java</files>
  <read_first>api-connector-spec/src/main/java/com/suntek/apiconnector/spec/ConnectorSpecParser.java, .planning/phases/02-data-mapping-engine/02-PATTERNS.md Pattern 2</read_first>
  <action>
    In `ConnectorSpecParser.parse()`: read top-level `mapping` map into `MappingSpec`; for each endpoint read `mappingOverride` into `MappingSpec`.
  Parse each direction block: if both `rules` and `script` present throw `IllegalArgumentException` with direction name (D-10).
    Implement `parseDirectionMapping`, `parseMappingRules`, `parseMappingSpec` private helpers.
    In `toConnectorMap()`: emit `mapping` when non-null/non-empty.
    In `endpointToMap()`: emit `mappingOverride` when present.
    Add `ConnectorSpecParserMappingTest`:
    (1) parse fixture YAML with mapping.request.rules rename op;
    (2) round-trip `parse → toConnectorMap → parse` preserves mapping block (MAP-05, D-27, D-28);
    (3) reject spec where `mapping.request` has both `rules` and `script`;
    (4) endpoint `mappingOverride.request.script` overrides connector declarative rules at model level (resolver tested in 02-03).
    Add test fixture `api-connector-mapping/src/test/resources/mapping/demo-rename.yaml` with minimal rename rule.
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-spec -am test -Dtest=ConnectorSpecParserMappingTest</verify>
  <acceptance_criteria>
    - ConnectorSpecParser.java contains mapping parse and serialize logic
    - ConnectorSpecParserMappingTest round-trip test passes
    - Mutual exclusion test throws on rules+script
    - demo-rename.yaml fixture exists under mapping test resources
  </acceptance_criteria>
  <done>Parser round-trips mapping for JDBC SPEC_JSON blob</done>
</task>

<task type="auto">
  <name>Task 3: MappingSpecValidator publish-time validation</name>
  <files>api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidator.java, api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/validation/MappingSpecValidatorTest.java</files>
  <read_first>api-connector-engine/src/main/java/com/suntek/apiconnector/engine/ResponseEvaluator.java, .planning/phases/02-data-mapping-engine/02-RESEARCH.md Publish Validation</read_first>
  <action>
    Implement `MappingSpecValidator` with `void validate(ConnectorSpec spec)`:
    For connector `mapping` and each endpoint `mappingOverride`, validate each configured direction.
    Re-assert rules XOR script per direction (D-10).
    For each rule: validate `op` is one of rename/coerce/nest/array_map/set; `set` requires `target`+`value`, no `source` (D-08); rename/coerce require `source`+`target`; coerce requires `type` in enum `string|number|boolean|date` (D-06).
    Compile each JSONPath in `source`/`target` via `JsonPath.compile(normalizePath(path))` — reuse normalizePath logic from ResponseEvaluator (bare field → `$.field`).
    Recursively validate nested rules inside `array_map.rules`.
    On failure throw `IllegalArgumentException` with actionable path like `mapping.request.rules[2].source` (D-04, D-30, ROADMAP SC#6).
    `MappingSpecValidatorTest`: (1) valid demo-rename spec passes; (2) invalid path `$.invalid[[` fails; (3) unknown op `flurp` fails; (4) coerce without type fails.
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-mapping -am test -Dtest=MappingSpecValidatorTest</verify>
  <acceptance_criteria>
    - MappingSpecValidator.java validates JSONPath via JsonPath.compile
    - MappingSpecValidatorTest rejects invalid JSONPath
    - Unknown transform op rejected
    - `.\mvnw-jdk21.ps1 -pl api-connector-mapping -am test` exits 0
  </acceptance_criteria>
  <done>Publish-time mapping schema validation ready for ConnectorPublishListener extension</done>
</task>

</tasks>

<verification>
- [ ] `.\mvnw-jdk21.ps1 -pl api-connector-mapping,api-connector-spec -am test` exits 0
- [ ] Mapping block round-trips through ConnectorSpecParser
- [ ] Invalid JSONPath rejected by MappingSpecValidator
</verification>

<success_criteria>
- api-connector-mapping module exists in BOM and builds
- MappingSpec model integrated into ConnectorSpec/EndpointSpec
- Parser + validator enforce D-01, D-03, D-04, D-10, D-27, D-30
- Foundation ready for DeclarativeRuleExecutor (Plan 02)
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Module |
|---|---|---|
| `api-connector-mapping` | Maven module | root |
| `MappingSpec` | record | api-connector-spec |
| `DirectionMappingSpec` | record | api-connector-spec |
| `MappingRule` | record | api-connector-spec |
| `MappingSpecValidator` | class | api-connector-mapping |
| `ConnectorSpecParserMappingTest` | test class | api-connector-spec |
| `MappingSpecValidatorTest` | test class | api-connector-mapping |
| `demo-rename.yaml` | fixture | api-connector-mapping test resources |

<output>
After completion, create `.planning/phases/02-data-mapping-engine/02-01-SUMMARY.md`
</output>
