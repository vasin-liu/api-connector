---
phase: 02-data-mapping-engine
plan: "05"
type: execute
wave: 5
depends_on:
  - "04"
files_modified:
  - api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/MappingEngineImpl.java
  - api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/ErrorMappingTrigger.java
  - api-connector-engine/src/main/java/com/suntek/apiconnector/engine/MappingConfigResolver.java
  - api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/ErrorMappingEngineTest.java
  - api-connector-engine/src/test/java/com/suntek/apiconnector/engine/MappingConfigResolverTest.java
  - api-connector-engine/src/test/java/com/suntek/apiconnector/engine/PassthroughMappingGuardTest.java
autonomous: true
requirements:
  - MAP-04
  - MAP-07
must_haves:
  truths:
    - "mapping.error rules transform vendor error JSON to LegacySuntekResult shape (D-16, MAP-04)"
    - "mapError invoked only when ErrorMappingTrigger indicates HTTP non-2xx or business failure (D-15, D-19)"
    - "When no mapping.error configured vendor error body passes through unchanged (D-18)"
    - "hasAnyMapping false for spec without mapping blocks — orchestrator must not call MappingEngine (D-23, D-26)"
    - "Passthrough is implicit — no mapping.mode field (D-24)"
  artifacts:
    - path: api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/ErrorMappingEngineTest.java
      provides: Vendor errCode/errMsg → legacy code/message/success:false
      min_lines: 50
    - path: api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/ErrorMappingTrigger.java
      provides: HTTP status + business success gate input for mapError
      contains: "shouldMapError"
    - path: api-connector-engine/src/test/java/com/suntek/apiconnector/engine/PassthroughMappingGuardTest.java
      provides: hasAnyMapping false + MappingEngine mock never called pattern
      min_lines: 30
  key_links:
    - from: MappingEngineImpl.mapError
      to: DeclarativeRuleExecutor
      via: mapping.error rules on vendor error body
      pattern: "mapError"
    - from: ErrorMappingTrigger
      to: ResponseEvaluator semantics
      via: httpStatus >= 400 OR !businessSuccess
      pattern: "shouldMapError"
    - from: PassthroughMappingGuardTest
      to: MappingConfigResolver.hasAnyMapping
      via: assert false when no mapping block
      pattern: "hasAnyMapping"
---

## Phase Goal

**As a** legacy API consumer, **I want** vendor errors mapped to platform business error JSON and simple endpoints to skip mapping entirely, **so that** drop-in compat is preserved without overhead on passthrough routes (MAP-04, MAP-07, D-15..D-19, D-23..D-26).

<objective>
Complete `mapError` direction, error trigger model, legacy shape tests, and passthrough `hasAnyMapping()` guard tests (orchestrator wiring deferred to Phase 3).

Purpose: ROADMAP SC#3 error mapping + SC#5 passthrough API.
Output: ErrorMappingEngineTest and PassthroughMappingGuardTest green.
</objective>

<execution_context>
@C:/Users/vasin/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/vasin/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-mapping-engine/02-CONTEXT.md
@.planning/phases/02-data-mapping-engine/02-PATTERNS.md
@api-connector-api/src/main/java/com/suntek/apiconnector/api/legacy/LegacySuntekResult.java
@api-connector-engine/src/main/java/com/suntek/apiconnector/engine/ResponseEvaluator.java
@api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/MappingEngineImpl.java
@api-connector-engine/src/main/java/com/suntek/apiconnector/engine/MappingConfigResolver.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorMappingTrigger and mapError implementation</name>
  <files>api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/ErrorMappingTrigger.java, api-connector-mapping/src/main/java/com/suntek/apiconnector/mapping/MappingEngineImpl.java, api-connector-mapping/src/test/java/com/suntek/apiconnector/mapping/ErrorMappingEngineTest.java</files>
  <read_first>api-connector-engine/src/main/java/com/suntek/apiconnector/engine/ResponseEvaluator.java, api-connector-api/src/main/java/com/suntek/apiconnector/api/legacy/LegacySuntekResult.java, .planning/phases/02-data-mapping-engine/02-PATTERNS.md Pattern 9</read_first>
  <action>
    Add `ErrorMappingTrigger` record: `int httpStatus`, `boolean businessSuccess` (from ResponseEvaluator gate).
    Static `boolean shouldMapError(ErrorMappingTrigger t)` — true when `httpStatus >= 400` OR `!businessSuccess` (D-15, D-19).
    Document: orchestrator calls mapError only when shouldMapError AND resolvedMapping.hasError(); Phase 3 wires gate.
    Complete `MappingEngineImpl.mapError(MappingContext ctx, ResolvedMapping config, ErrorMappingTrigger trigger)`:
    If `!config.hasError()` return `ctx.rawBody()` unchanged (D-18 passthrough vendor error).
    If `!shouldMapError(trigger)` throw IllegalStateException or return rawBody — caller responsibility in Phase 3.
    Apply declarative `mapping.error` rules OR error script via same dispatch as request/response.
    `ErrorMappingEngineTest`:
    (1) vendor `{"errCode":"500","errMsg":"vendor fail"}` + error rules rename to `code`/`message` + set `success:false` → matches LegacySuntekResult field names (D-16);
    (2) HTTP 200 + business failure trigger → error mapping applied;
    (3) HTTP 200 + business success → mapError not applicable (test trigger false path returns passthrough);
    (4) no mapping.error configured → vendor body unchanged (D-18);
    (5) Groovy error script path if ResolvedMapping has error script.
    Assert output JSON deserializes to LegacySuntekResult with success=false.
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-mapping -am test -Dtest=ErrorMappingEngineTest</verify>
  <acceptance_criteria>
    - ErrorMappingTrigger.shouldMapError handles HTTP 4xx/5xx and business failure
    - ErrorMappingEngineTest maps vendor error to legacy shape
    - Passthrough when no mapping.error configured
    - MAP-04 satisfied
    - ROADMAP SC#3 proven in unit test
  </acceptance_criteria>
  <done>Error response mapping produces legacy compat business error JSON</done>
</task>

<task type="auto">
  <name>Task 2: Endpoint error mapping override resolution</name>
  <files>api-connector-engine/src/main/java/com/suntek/apiconnector/engine/MappingConfigResolver.java, api-connector-engine/src/test/java/com/suntek/apiconnector/engine/MappingConfigResolverTest.java</files>
  <read_first>api-connector-engine/src/main/java/com/suntek/apiconnector/engine/MappingConfigResolver.java, .planning/phases/02-data-mapping-engine/02-CONTEXT.md D-17</read_first>
  <action>
    Extend `MappingConfigResolverTest` for error direction (D-17 connector default + endpoint override):
    (1) connector `mapping.error` rules used when endpoint has no error override;
    (2) endpoint `mappingOverride.error` replaces connector error rules entirely;
    (3) endpoint override with only request script does not clear connector error rules (per-direction independence).
    Ensure `ResolvedMapping.hasError()` reflects merged resolution correctly.
    Add integration-style test in mapping module: resolve config + mapError produces endpoint-specific error template.
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-engine -am test -Dtest=MappingConfigResolverTest</verify>
  <acceptance_criteria>
    - Endpoint error override wins over connector error mapping
    - Per-direction override independence proven
    - D-01 and D-17 honored
  </acceptance_criteria>
  <done>Error mapping resolution follows connector default + endpoint override</done>
</task>

<task type="auto">
  <name>Task 3: Passthrough hasAnyMapping guard tests</name>
  <files>api-connector-engine/src/test/java/com/suntek/apiconnector/engine/PassthroughMappingGuardTest.java, api-connector-engine/src/test/java/com/suntek/apiconnector/engine/MappingConfigResolverTest.java</files>
  <read_first>.planning/phases/02-data-mapping-engine/02-PATTERNS.md Pattern 10, .planning/phases/02-data-mapping-engine/02-CONTEXT.md D-23..D-26</read_first>
  <action>
    Extend `MappingConfigResolverTest`:
    (4) ConnectorSpec with only `transform[]` and no `mapping` → `hasAnyMapping()==false` (D-25: transform does not imply mapping).
    (5) ConnectorSpec with empty mapping object `{}` → `hasAnyMapping()==false` (D-24 implicit passthrough).
    Add `PassthroughMappingGuardTest` documenting Phase 3 orchestrator contract (D-26):
    Pseudocode test with Mockito: given spec without mapping, simulate orchestrator guard `if (hasAnyMapping) mappingEngine.mapRequest` — verify MappingEngine never invoked.
    Assert comment references DefaultIntegrationOrchestrator future wiring — **do not modify orchestrator invoke path in Phase 2**.
    Document: passthrough skips JSON mapping only; `transform[]` still runs when configured (D-25).
    MAP-07 satisfied at API level; true zero overhead proven in Phase 3 orchestrator test.
  </action>
  <verify>.\mvnw-jdk21.ps1 -pl api-connector-engine -am test -Dtest=PassthroughMappingGuardTest,MappingConfigResolverTest</verify>
  <acceptance_criteria>
    - hasAnyMapping returns false when no mapping.* blocks
    - transform[] alone does not set hasAnyMapping true
    - PassthroughMappingGuardTest documents orchestrator short-circuit without wiring invoke
    - No DefaultIntegrationOrchestrator.invoke changes
    - MAP-07 API requirement satisfied
  </acceptance_criteria>
  <done>Passthrough detection API ready for Phase 3 orchestrator short-circuit</done>
</task>

</tasks>

<verification>
- [ ] `.\mvnw-jdk21.ps1 -pl api-connector-mapping,api-connector-engine -am test` exits 0
- [ ] Error mapping produces LegacySuntekResult-compatible JSON
- [ ] hasAnyMapping false for passthrough specs
</verification>

<success_criteria>
- MAP-04 error JSON maps to legacy business error structure
- MAP-07 passthrough mode detected via hasAnyMapping without mapping.mode field
- Error trigger matrix D-15, D-18, D-19 implemented in engine API
</success_criteria>

## Artifacts this phase produces

| Symbol / File | Kind | Module |
|---|---|---|
| `ErrorMappingTrigger` | record | api-connector-mapping |
| `mapError` (complete) | method | MappingEngineImpl |
| `ErrorMappingEngineTest` | test class | api-connector-mapping |
| `PassthroughMappingGuardTest` | test class | api-connector-engine |
| `MappingConfigResolverTest` (extended) | test class | api-connector-engine |

<output>
After completion, create `.planning/phases/02-data-mapping-engine/02-05-SUMMARY.md`
</output>
