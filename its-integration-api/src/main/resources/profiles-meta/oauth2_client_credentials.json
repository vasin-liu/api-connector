{
  "profileId": "oauth2_client_credentials",
  "level": "L1",
  "displayName": "OAuth2 Client Credentials",
  "description": "标准 client_credentials 换 Token",
  "credentialSlots": [
    { "key": "appId", "label": "Client ID", "secret": false, "required": true },
    { "key": "appSecret", "label": "Client Secret", "secret": true, "required": true }
  ],
  "paramSchema": {
    "type": "object",
    "properties": {
      "tokenUrl": { "type": "string", "title": "Token 路径" },
      "scope": { "type": "string", "title": "Scope" }
    },
    "required": ["tokenUrl"]
  },
  "specFragmentExample": {
    "type": "oauth2_client_credentials",
    "tokenUrl": "/oauth/token",
    "scope": ""
  }
}
