{
  "profileId": "oauth2_token_in_query",
  "level": "L2",
  "displayName": "OAuth2 Token 拼 Query",
  "description": "client_credentials 换 Token 后写入 URL 查询参数（百度文心）",
  "credentialSlots": [
    { "key": "appId", "label": "API Key / Client ID", "secret": false, "required": true },
    { "key": "appSecret", "label": "Secret Key", "secret": true, "required": true }
  ],
  "paramSchema": {
    "type": "object",
    "properties": {
      "tokenUrl": { "type": "string", "default": "/oauth/2.0/token" },
      "tokenParam": { "type": "string", "default": "access_token" }
    }
  },
  "specFragmentExample": {
    "type": "oauth2_token_in_query",
    "tokenUrl": "/oauth/2.0/token",
    "tokenParam": "access_token"
  }
}
